RSpec.describe Debsources::Watch::Client do
  it "has a version number" do
    expect(Debsources::Watch::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
